version: '3.8'

services:
  app:
    build: .
    container_name: snipe-giveaway
    ports:
      - "3000:3000"
    volumes:
      # Persist database between container restarts
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
